name: Debug Secrets (Safe)
on: [push]

jobs:
  debug-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Print available secrets (Masked)
        run: |
          echo "Available secrets:"
          for var in $(env | grep '^SECRET_' | cut -d= -f1); do
            echo "$var=[MASKED]"
          done
